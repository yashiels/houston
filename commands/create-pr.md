---
description: "Create a PR (GitHub) or MR (GitLab) with correct format, reviewers, squash, auto-merge"
argument-hint: "<TICKET-ID> [--type feat|fix|chore] [--area <area>] [--title <title>]"
---

Run `$HOUSTON_DIR/scripts/create-pr.sh $ARGUMENTS` and display the result.

If `HOUSTON_DIR` is not set, try `~/Developer/houston`.
