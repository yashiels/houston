---
description: "Resume a failed or crashed Houston pipeline"
argument-hint: "<TICKET-ID>"
---

Run `$HOUSTON_DIR/pipeline/resume.sh $ARGUMENTS` and display the output.

If `HOUSTON_DIR` is not set, try `~/Developer/houston`.
