---
description: "Create MRs/PRs across multiple repos in dependency order"
argument-hint: "<TICKET-ID> --repos <path1> <path2> ..."
---

Run `$HOUSTON_DIR/scripts/create-multi-repo-mr.sh $ARGUMENTS` and display the summary.

If `HOUSTON_DIR` is not set, try `~/Developer/houston`.
