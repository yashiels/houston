---
description: "Check CI/CD pipeline status for current branch"
argument-hint: "[--branch <name>] [--wait]"
---

Run `$HOUSTON_DIR/scripts/check-pipeline.sh $ARGUMENTS` and display the results.

Summarize: which jobs passed, which failed, and link to failed job logs.

If `HOUSTON_DIR` is not set, try `~/Developer/houston`.
